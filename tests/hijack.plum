import "std/io"

proc add ( qword qword ) [hijack] qword :
    [hijack] "add x0, x0, x1" ;
    [hijack] { "ldp x29, x30, [sp], #16" "ret" } ;
end

proc main ( qword ptr ) [hijack] qword :
    "Hello World!\n" @stdout $0 fprintf!
    1 2 add! "%ld" $1 @println
    0 return
end
