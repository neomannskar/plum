import "std/io"
import "std/lib"

proc main :
    32 alloc

    if dup @NULL ==
    do
        "Failed to allocate memory" "%s" @println
    else end

    "Allocation succeeded!" "%s" @println

    free
    0 return
end
